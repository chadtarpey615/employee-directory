<nav class="flex h-14 bg-slate-600 items-center">
    <div class="container mx-auto px-4">
      <div class="flex justify-between">
        <div class="flex space-x-4">
                <!-- Logo -->
          <div>
            <%= link_to "Employee Directory", root_path, class: "text-white text-xl font-bold" %>
          </div>
            
        </div>
         <div class="hidden md:flex items-center space-x-1">
                <%= link_to "Employees", employees_path, class: "text-white hover:bg-slate-700 px-3 py-2 rounded" %>

                <% if user_signed_in? %>
                <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "text-white hover:bg-slate-700 px-3 py-2 rounded" %>
                <% else %>
                <%= link_to "Login", new_user_session_path, class: "text-white hover:bg-slate-700 px-3 py-2 rounded" %>
                <% end %>
            </div>
        </div>
    </div>

    <!--  mobile menu -->
    <div class="md:hidden">
        <button class="mobile-menu-button flex items-center px-3 py-2 border rounded text-slate-200 border-slate-400 hover:text-white hover:border-white mx-4">
            <svg class="fill-current h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><title>Menu</title><path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/></svg>
        </button>

        <div class="mobile-menu hidden">
            <ul>
                <li><%= link_to "Employees", employees_path, class: "block text-white hover:bg-slate-700 px-3 py-2 rounded" %></li>
                <% if user_signed_in? %>
                <li><%= link_to "Logout", destroy_user_session_path, method: :delete, class: "block text-white hover:bg-slate-700 px-3 py-2 rounded" %></li>
                <% else %>
                <li><%= link_to "Login", new_user_session_path, class: "block text-white hover:bg-slate-700 px-3 py-2 rounded" %></li>
                <% end %>
            </ul>
    </div>   

</nav>